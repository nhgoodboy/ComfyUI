requests>=2.20.0
websocket-client>=1.0.0
fastapi
uvicorn[standard]
pydantic
aiohttp
websockets

# Pillow is optional, only if image manipulation/creation is needed directly in client examples
# Pillow>=9.0.0

# For ComfyUI interaction (if its client library is pip-installable, add here)
# comfyui-client-library # Placeholder, replace with actual if available

# For development and testing
# pytest
# httpx # For testing FastAPI async endpoints