"""
前端双图片上传功能测试指南

这个文档描述了如何手动测试双图片上传功能
"""

# 前端双图片上传功能测试步骤

## 1. 启动服务

### 启动ComfyUI工作流服务器
```bash
cd /mnt/d/workspace/ComfyUI/witness/comfyui_workflow_server
python -m uvicorn app.main:app --host 0.0.0.0 --port 8001 --reload
```

### 启动前端Web服务
```bash
cd /mnt/d/workspace/ComfyUI/witness/web_image_transform
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

## 2. 测试功能

### 2.1 访问前端页面
打开浏览器访问: http://localhost:8000

### 2.2 测试单图片模式（默认）
1. 页面应该显示单个上传区域
2. 选择风格下拉菜单中的"黏土风格转换"或"动漫风格转换"
3. 验证只显示一个图片上传区域
4. 上传一张测试图片，确认预览正常

### 2.3 测试双图片模式
1. 在风格下拉菜单中选择"人物场景融合"
2. 页面应该自动切换到双图片上传界面
3. 验证显示两个上传区域：
   - 图片1 (人物)
   - 图片2 (场景)
4. 分别上传两张测试图片
5. 验证两个图片都有预览显示
6. 确认"开始转换"按钮只有在两张图片都上传后才可点击

### 2.4 测试模式切换
1. 从"人物场景融合"切换回"黏土风格转换"
2. 验证界面切换回单图片模式
3. 验证之前上传的图片被清理
4. 再次切换到"人物场景融合"
5. 验证界面又切换到双图片模式

## 3. 验证点

### 前端界面验证
- [ ] 单图片模式显示正确
- [ ] 双图片模式显示正确  
- [ ] 模式切换流畅无错误
- [ ] 图片预览正常显示
- [ ] 提交按钮状态正确
- [ ] 文件名显示正确

### 后端处理验证
- [ ] 样式配置正确加载
- [ ] API接口支持双文件参数
- [ ] 参数验证正确工作
- [ ] RPC方法路由正确

### JavaScript控制台验证
打开浏览器开发者工具，检查：
- [ ] 无JavaScript错误
- [ ] 上传模式切换日志正确
- [ ] 文件选择事件正确触发
- [ ] AJAX请求参数正确

## 4. 常见问题排查

### 如果双图片界面不显示
1. 检查JavaScript控制台是否有错误
2. 确认person_scene_merge在样式列表中
3. 检查HTML模板是否正确加载

### 如果样式选择无效
1. 检查comfyui_workflow_server是否正常运行
2. 确认style_configs.yaml配置正确
3. 检查样式注册表是否正确加载

### 如果图片上传失败
1. 检查文件大小是否超过10MB限制
2. 确认文件类型是图片格式
3. 检查网络连接和服务器状态

## 5. 测试图片建议

推荐使用以下类型的测试图片：
- 人物图片：清晰的人物照片，PNG或JPG格式，1-5MB
- 场景图片：风景、建筑等背景图片，PNG或JPG格式，1-5MB

## 6. 预期结果

成功的测试应该看到：
1. 界面根据选择的风格正确切换单/双图片模式
2. 图片上传和预览功能正常
3. 提交按钮状态根据文件上传情况正确更新
4. 没有JavaScript错误或网络错误
5. 后端API正确接收和处理请求